我们拿到一个APP 逆向过程是基本过程是什么呢

我们使用`嘟嘟牛在线 ` 这个APK 进行基本操作

# 抓包

那么能够正常打开 我们就可以进行一个基本的抓包 我们使用 HTTPCanary抓包

![image-20260326123357250](https://raw.githubusercontent.com/Xioaruan912/pic/main/image-20260326123357250.png)

那么这个明显的数据包中存在加密内容 所以我们可以分析加密过程是如何的 

# 分析框架

这里我们需要分析 这个APK 是如何开发的 有可能是H5的APK

那么我们要分析是哪里进行发包的 有可能是JS加密 有可能是Java加密

我们需要了解这个 框架开发是什么

那么我们如何查看呢 我们可以使用SDK 自带的工具 `uiautomatorviewer`查看

如果没有 需要去下载

![image-20260326124756768](https://raw.githubusercontent.com/Xioaruan912/pic/main/image-20260326124756768.png)

下载完毕后到 SDK目录

```
C:\Users\用户\AppData\Local\Android\Sdk\tools\bin
```

打开后 我们可以通过USB 截图 分析

![image-20260326125510623](https://raw.githubusercontent.com/Xioaruan912/pic/main/image-20260326125510623.png)

可以发现都是组件 说明是至少不是H5的APK 所以加密过程不是在 JS文件中 因为不是通过Node构建的

## 应对

通过禁止截屏 就可以应对

```
activity.getWindow().addFlags(WindowManager.LayoutParams.FLAG_SECURE);
```

这个方法就可以应对这个检测

那么我们可以HOOK这个函数 或者使用取消禁止截屏

```
activity.getWindow().clearFlags(WindowManager.LayoutParams.FLAG_SECURE);
```

这里都是Java相关的代码

上述方法定位结束后 没有加壳什么的我们就可以直接反编译代码了

# 查壳

我们其实可以通过 `PKID` 查壳看看 有没有加固

![image-20260326130754155](https://raw.githubusercontent.com/Xioaruan912/pic/main/image-20260326130754155.png)

可以发现没有加固 那么我们就开始反汇编

# 反汇编 加密代码分析

当然首先去看`AndroidManifest.xml`

可以发现 定位到了我们的入口函数

![image-20260326131155313](https://raw.githubusercontent.com/Xioaruan912/pic/main/image-20260326131155313.png)

当然 我们目前只是看加密过程 那么我们不需要细致分析 

## 控件ID定位

我们之前通过`uiautomatorviewer` 可以得到登入的控件ID 我们只需要去搜索即可

## 人工搜索

我们只需要搜索我们需要的内容 比如这里的关键字`手机号` 我们可以定位到这里

![image-20260326131558563](https://raw.githubusercontent.com/Xioaruan912/pic/main/image-20260326131558563.png)

那么我们可以看到 他就是登入的界面

```
public class LoginActivity extends BasicActivity implements View.OnClickListener {
```

然后就是初始化我们的界面 找到这两个输入

```

    private void initView() {
        this.etMobile = (EditText) findViewById(R.id.et_mobile);
        this.etPwd = (EditText) findViewById(R.id.et_pwd);
        if (this.para == null) {
            this.para = new HashMap();
        }
    }

```

最后就是开始监听

```
    private void initEvent() {
        findViewById(R.id.btn_login).setOnClickListener(this);
        findViewById(R.id.btn_forget_password).setOnClickListener(this);
        findViewById(R.id.btn_register_now).setOnClickListener(this);
    }
```

那么点击的处理就在`onClick` 中 我们着重查看 登入的

```
            case R.id.btn_login /* 2131558593 */:
            	//手机密码 得到并且去除 空格
                String mobile = ((Object) this.etMobile.getText()) + "".trim();
                String pwd = ((Object) this.etPwd.getText()) + "".trim();
                //用户输入的内容隐藏
                Utils.hideSoftInput(this, this.etPwd);
                if (checkInput(mobile, pwd)) {
                    login(mobile, pwd);
                    break;
                }
                break;

```

然后就是进入` checkInput` 没啥特别的 可以发现是 通过`Toast` 输出

```

    private boolean checkInput(String mobile, String pwd) {
        String msg = "";
        if (TextUtils.isEmpty(mobile)) {
            msg = "手机号码不能为空";
        } else if (TextUtils.isEmpty(pwd)) {
            msg = "登录密码不能为空";
        }
        if (TextUtils.isEmpty(msg)) {
            return true;
        }
        showToast(msg);
        return false;
    }

```

下面就是进入登入逻辑

```

    private void login(String userName, String pwd) {
        this.DEFAULT_TYPE = new TypeToken<RequestResult<User>>() { // from class: com.dodonew.online.ui.LoginActivity.1
            AnonymousClass1() {
            }
        }.getType();
        this.para.clear();
        this.para.put("username", userName);
        this.para.put("userPwd", pwd);
        if (TextUtils.isEmpty(DodonewOnlineApplication.devId)) {
            DodonewOnlineApplication.devId = Utils.getDevId(DodonewOnlineApplication.getAppContext());
        }
        this.para.put("equtype", Config.equtype);
        this.para.put("loginImei", "Android" + DodonewOnlineApplication.devId);
        requestNetwork("user/login", this.para, this.DEFAULT_TYPE);
    }

```

可以发现 把一些数据都写入了 `this.para` 传递给了`requestNetwork` 

    private void requestNetwork(String cmd, Map<String, String> para, Type type) {
        showProgress();
        String url = "http://api.dodovip.com/api/" + cmd;
        this.request = new JsonRequest(this, url, "", new Response.Listener<RequestResult>() { // from class: com.dodonew.online.ui.LoginActivity.2
            final /* synthetic */ String val$cmd;
    
            AnonymousClass2(String cmd2) {
                str = cmd2;
            }
    
            /* JADX WARN: Multi-variable type inference failed */
            @Override // com.android.volley.Response.Listener
            public void onResponse(RequestResult requestResult) {
                if (requestResult.code.equals(a.e)) {
                    if (str.equals("user/login")) {
                        DodonewOnlineApplication.loginUser = (User) requestResult.data;
                        DodonewOnlineApplication.loginLabel = "mobile";
                        Utils.saveJson(LoginActivity.this, DodonewOnlineApplication.loginLabel, Config.LOGINLABEL_JSON);
                        LoginActivity.this.intentMainActivity();
                    }
                } else {
                    LoginActivity.this.showToast(requestResult.message);
                }
                LoginActivity.this.dissProgress();
            }
        }, this, type);
        this.request.addRequestMap(para, 0);
        DodonewOnlineApplication.addRequest(this.request, this);
    }

那么这里我们只需要关注我们的 参数是什么加密的 所以可以定位到

`this.request.addRequestMap(para, 0);`

我们进入查看

```
    public void addRequestMap(Map<String, String> addMap, int a) {
        String time = System.currentTimeMillis() + "";
        if (addMap == null) {
            addMap = new HashMap<>();
        }
        addMap.put("timeStamp", time);
        String code = RequestUtil.paraMap(addMap, Config.BASE_APPEND, "sign");
        String encrypt = RequestUtil.encodeDesMap(code, this.desKey, this.desIV);
        JSONObject obj = new JSONObject();
        try {
            obj.put("Encrypt", encrypt);
            this.mRequestBody = obj + "";
        } catch (JSONException e) {
            e.printStackTrace();
        }
    }

```

ok咯 关键点来了

```
        String encrypt = RequestUtil.encodeDesMap(code, this.desKey, this.desIV);
```

那么我们 处理一下这个

```
    addMap.put("timeStamp", time);
    public static final String BASE_APPEND = "sdlkjsdljf0j2fsjk";
	String code = RequestUtil.paraMap(addMap, Config.BASE_APPEND, "sign");
```

准备好了就进入DES加密

```
    public static final String BASE_DES_IV = "32028092";
    public static final String BASE_DES_KEY = "65102933";
    this.desKey = Config.BASE_DES_KEY;
    this.desIV = Config.BASE_DES_IV;
    String encrypt = RequestUtil.encodeDesMap(code, this.desKey, this.desIV);

```

那么完整的加密过程不就结束了吗

所以如果只是对APK的逆向 那么这个就完全结束 构造Python代码 即可访问

那么其实上述是传统的内容 我们一般都是通过Hook常用的系统函数 从而定位

