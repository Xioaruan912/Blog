这里买了一台物语云的服务器 进行配置

推荐dd完毕后操作 国内机器都推荐dd

```
curl -O https://raw.githubusercontent.com/bin456789/reinstall/main/reinstall.sh || wget -O reinstall.sh $_
bash reinstall.sh debian 13
```

给出一个[Github](https://ghproxy.link/)

```
https://ghfast.top
```

```
apt update && apt install -y curl wget unzip git screen
apt-get install -y sudo
```

```
git clone https://ghfast.top/https://github.com/nelvko/clash-for-linux-install.git \
  && cd clash-for-linux-install \
  && sudo bash install.sh
```

安装python

```
curl -sSL -o miniconda.sh "https://raw.githubusercontent.com/Xioaruan912/Xcript/main/sh/miniconda.sh" && chmod +x miniconda.sh && ./miniconda.sh && rm miniconda.sh
```

这样就差不多可以享受 国内机器了
