这里说的是项目结构和基本的部件的理解

# 项目结构

![image-20260324180723073](https://raw.githubusercontent.com/Xioaruan912/pic/main/image-20260324180723073.png)

这个列表中存在我们的配置环境

![image-20260324180737667](https://raw.githubusercontent.com/Xioaruan912/pic/main/image-20260324180737667.png)

SDK也就是我们开发安卓所使用的库函数

NDK是我们开发so文件所使用的 是arm平台的 所以需要交叉编译

我们可以去下载

![image-20260324181031684](https://raw.githubusercontent.com/Xioaruan912/pic/main/image-20260324181031684.png)

那其实我们之前学习了 我们工程下面存在模块 一个模块就对应一个APK 所以我们看看模块的架构是什么

![image-20260324181539698](https://raw.githubusercontent.com/Xioaruan912/pic/main/image-20260324181539698.png)

`build` 就是我们构建的APK以及中间文件保存的地方

`libs` 是我们开发次模块的时候 第三方的`jar包`

`src` 就是我们的源代码

![image-20260324181748810](https://raw.githubusercontent.com/Xioaruan912/pic/main/image-20260324181748810.png)

有3个目录 除了main 其他都是测试文件 我们其实可以不需要管理 我们主要编写代码 就是在` main`中 

![image-20260324181828983](https://raw.githubusercontent.com/Xioaruan912/pic/main/image-20260324181828983.png)

其中 java目录中的代码 会被编译到` cless.dex` 中

`AndroidManifest.xml` 就是我们APK 的配置文件

`res` 就是我们的图标 布局相关的目录 

![image-20260324182025422](https://raw.githubusercontent.com/Xioaruan912/pic/main/image-20260324182025422.png)

点开后可以通过类似MFC 方式 组织前端代码

大概完整框架差不多就是这样 那么下面就是细致的内容

# build.gradle

可以发现我们项目中存在多个`build.gradle` 所以我们具体解释一下这个文件

我们可以看看 `main` 中的 我们拆分查看

![image-20260324182426555](https://raw.githubusercontent.com/Xioaruan912/pic/main/image-20260324182426555.png)

```
plugins {
    //构建系统要使用哪些功能插件
    id 'com.android.application'
}
```

```
android {
	//这里是我们启动工程起的名字
    namespace 'com.example.testapplication'
    
    // 编译所使用的SDK版本是什么
    compileSdk 33



    defaultConfig {
    	//APK的包名
        applicationId "com.example.testapplication"
        //最小运行SDK
        minSdk 16
        //希望在那个目标SDK下运行
        targetSdk 33
        //APK的版本
        versionCode 1
        //APK版本的名字
        versionName "1.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }
	//对于编译的设置
    buildTypes {
        release {
        	//是否开启混淆
            minifyEnabled false
            //如果开启混淆 那么要在依据哪个混淆文件
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
    buildFeatures {
        viewBinding true
    }
}
```

```
//依赖库
dependencies {

    implementation 'androidx.appcompat:appcompat:1.4.1'
    implementation 'com.google.android.material:material:1.5.0'
    implementation 'androidx.constraintlayout:constraintlayout:2.1.3'
    implementation 'androidx.navigation:navigation-fragment:2.4.1'
    implementation 'androidx.navigation:navigation-ui:2.4.1'
    testImplementation 'junit:junit:4.13.2'
    androidTestImplementation 'androidx.test.ext:junit:1.1.3'
    androidTestImplementation 'androidx.test.espresso:espresso-core:3.4.0'
}
```

总的来说`gradle` 就是指导我们的 编译模块如何进行的

# AndroidManifest.xml

这个可能是我们反编译后 第一时间查看的文件

```
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
    	//是否允许备份
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        //手机上的图标
        android:icon="@mipmap/ic_launcher"
        //屏幕上显示的名字
        android:label="@string/app_name"
        //是否支持从右往左排序
        android:supportsRtl="true"
        //主题是什么
        android:theme="@style/Theme.TestApplication"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TestApplication.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
```

`application` ： 是我们APK 自身的属性 其中主要的内容是`android:name=".MainActivity"` 

这个属性规定了我们的函数执行入口

在`application` 中依旧包含子节点 并且其实一般是4大组件

那么上面的xml中注册了一个`activity ` 也就是一个界面 描述了我们这个界面绑定是`android:name=".MainActivity"`

那么我们清单文件最简单的了解差不多就是这些 最基础的我们能看明白就先行了

# 安卓程序的入口

我们开启一个APK 是从哪里开始执行呢 这也是我们逆向的过程

首先查看xml文件 首先定位`Activity` 界面并且是带有

```
<category android:name="android.intent.category.LAUNCHER" />
```

这个一般是主界面 那么这个绑定的是哪个类 就是入口文件 入口文件代码如下：

![image-20260324184530622](https://raw.githubusercontent.com/Xioaruan912/pic/main/image-20260324184530622.png)

我们可以添加`log.d` 顺便复习一下我们的` logcat`

```
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        Log.d("ccccccc","onCreate");
        binding = ActivityMainBinding.inflate(getLayoutInflater());
        setContentView(binding.getRoot());

        setSupportActionBar(binding.toolbar);

        NavController navController = Navigation.findNavController(this, R.id.nav_host_fragment_content_main);
        appBarConfiguration = new AppBarConfiguration.Builder(navController.getGraph()).build();
        NavigationUI.setupActionBarWithNavController(this, navController, appBarConfiguration);

        binding.fab.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View view) {
                Snackbar.make(view, "Replace with your own action", Snackbar.LENGTH_LONG)
                        .setAnchorView(R.id.fab)
                        .setAction("Action", null).show();
            }
        });
    }
```

在`onCreate` 中添加日志相关代码`Log.d("ccccccc","onCreate");

![image-20260324192618225](https://raw.githubusercontent.com/Xioaruan912/pic/main/image-20260324192618225.png)

但是 如果我们的程序是在 `application`  就继承了某类 那么这个类就是入口函数

![image-20260324193039511](https://raw.githubusercontent.com/Xioaruan912/pic/main/image-20260324193039511.png)

我们构建一下这个 类

```
package com.example.myapplication;

import android.app.Application;
import android.content.Context;
import android.util.Log;

public class appTest extends Application {

    static {
        Log.d("CCCCCCCCCCCCCC","testAPK static");
    }

    @Override
    protected void attachBaseContext(Context base) {
        super.attachBaseContext(base);
        Log.d("CCCCCCCCCCCCCC","testAPK attachBaseContext");
    }

    @Override
    public void onCreate() {
        super.onCreate();
        Log.d("CCCCCCCCCCCCCC","testAPK onCreate");
    }
}

```

现在开始测试就可以知道哪里先输出了

![image-20260324193153904](https://raw.githubusercontent.com/Xioaruan912/pic/main/image-20260324193153904.png)

一般加壳程序会在 `application`  执行 因为这个是我们程序员可以控制的最早时机就在这里

并且 `application` 的生命周期很长 所以可以传递一些全局变量
