# Java对象的内存管理

在构造对象的过程中 一定会用到 方法区 扎内存和堆内存

![image-20260120162725477](https://raw.githubusercontent.com/Xioaruan912/pic/main/image-20260120162725477.png)

在`new`一个对象过程中 方法区 会加载 `student`的 `class文件` 并且内部保存着 全部的属性和方法

我们也知道 `new` 返回的是一个对象的 地址  所以会在`main`的栈中 保存一个 变量

![image-20260120162859149](https://raw.githubusercontent.com/Xioaruan912/pic/main/image-20260120162859149.png)

堆内存中 `new `后保存上述内容

# 对象在方法中的传递过程

如果我们 通过方法传递一个 对象 我们知道 传递的就是 对象的地址 所以我们在方法内部修改 那么就会直接修改对象的属性

# `this`关键字的本质

表示 所在方法调用者的地址值