# MACOS

```
curl -s "https://get.sdkman.io" | bash
source "$HOME/.sdkman/bin/sdkman-init.sh"
```

```
sdk list java          # 查看可用 JDK 列表
sdk install java 21.0.4-tem   # 安装指定版本
sdk use java 21.0.4-tem       # 仅当前终端启用
sdk default java 21.0.4-tem   # 设为全局默认
```

